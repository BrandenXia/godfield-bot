# God Field current-state survey — 2026-09-06

## Scope and provenance

This is a read-only survey of the live English web client at
<https://godfield.net/> and its public store listings. The in-game Bible was
opened in the live client and all 291 artifact detail cards were inspected.
The machine-readable manifest is in
[`data/snapshots/2026-09-06/manifest.json`](../../data/snapshots/2026-09-06/manifest.json).
The complete normalized extraction is in
[`data/snapshots/2026-09-06/bible.json`](../../data/snapshots/2026-09-06/bible.json).

Observed at approximately `2026-09-06T17:02:25Z`:

- Web bundle: `https://godfield.net/main.dart.js`
- SHA-256: `764a50524e4b6b3f510415da7128abd8ad99dcb87b45b8572d98ddd96889cabd`
- HTTP ETag: `2a2f9f9759840e21fb6391029ca2638e4a2ce6fa99e0869251eed00b6344374f`
- HTTP Last-Modified: `Tue, 11 Aug 2026 01:40:09 GMT`
- App Store release: `1.5.27`, dated Aug 14, 2026
- Google Play listing last updated: Aug 11, 2026

The store dates are useful freshness signals, but the web bundle hash is the
compatibility key the automation should record for every run.

### Freshness revalidation — 2026-09-07

At `2026-09-07T04:40:00Z`, the public Bible was extracted again through the
live browser UI into ignored local storage. The client SHA-256 remained
`764a50524e4b6b3f510415da7128abd8ad99dcb87b45b8572d98ddd96889cabd`,
all seven category counts still totaled 291, and a typed semantic diff found
no client, source, language, reference-rule, category-note, or artifact-record
changes from the accepted snapshot.

A second extraction at `2026-09-07T04:52:07Z` extended each artifact record
with official element-icon paths from its detail panel. It found element icons
on 145 records, including 54 weapons. Frozen Hammer, for example, carries
`/images/elements/water.webp` despite its text-only detail looking like a plain
`ATK4` weapon. The executable neutral-attack allowlist now requires no element
icons and initially contained 25 weapons rather than the earlier text-only set
of 46. The
enriched [Bible snapshot](../../data/snapshots/2026-09-07/bible.json) and its
[manifest](../../data/snapshots/2026-09-07/manifest.json) are versioned here.

## Client and account behavior

- The web client is a Dart application rendered as ordinary, absolutely
  positioned DOM elements in a logical 1080×720 viewport. It is not a canvas.
  Text, inputs, image asset paths, and click targets can therefore be observed
  without OCR in the current build.
- The public page loads `main.dart.js`, Firebase 12.17.1 modules, and Google
  Analytics. The client bundle initializes Firebase anonymous authentication,
  Firestore, and callable cloud functions.
- The entry screen asks for a Prophet Name (maximum 18 characters) and offers
  Genesis. The requested name `ロキ-67` fits.
- The current English menu labels the modes Training (`vs Computers`), Hidden
  Melee (`vs Friends`), and Royal Duel (`Solo 2 players`). The official store
  description says the game supports up to nine players.
- No public API documentation or automation policy was found on the official
  website or either store listing. Internal service names visible in a shipped
  client are not a public API contract and should not be used by the bot.
- The official website exposes a privacy policy and an account-deletion
  control, but no separate terms/automation page was located. Absence of such
  a page is not permission for unattended public matchmaking.

The identity is effectively a Firebase anonymous account tied to browser
storage, not a conventional username/password login. A consistent bot should
therefore use one dedicated persistent Chromium profile. That profile is a
credential: it must live outside Git with owner-only permissions and be backed
up only through an encrypted, explicit operator flow.

Reopening the persistent profile returns to the Prophet Name screen rather
than the mode menu. Genesis is therefore a per-browser-session entry step, not
proof that a new anonymous identity is being created. The adapter fingerprints
the stored anonymous UID in-page with SHA-256, records only that one-way value,
and refuses to continue if it changes.

The current Training flow opens a room, then exposes `2 trainees`, an
Apocalypse selector defaulting to `None`, and `Start Battle` after the computer
joins. No friend-room or Royal Duel control is used by the Training adapter.

An initial two-player Training battle was observed at `G.F.0`. Both player rows
exposed name, HP, MP, and money as aligned DOM text. The local hand exposed nine
80×80 item assets at a shared y-coordinate; hovering rendered a separate detail
copy to the right. The normalizer groups the hand spatially, excludes that
detail copy, and preserves the DOM slot order. Background layers included both
`/images/screens/room.webp` and `/images/screens/fog.webp`, so screen identity
must use stronger gameplay evidence before persistent background assets.

Every hand image in the observed battle had an adjacent transparent hit target
with identical 80×80 bounds; the hover-detail copy had no such target. That is
the initial artifact-selection contract. Closing a browser during Training can
leave a one-trainee room that is restored on the next Genesis. The adapter can
recover through the unique clickable header control immediately left of the
`Training` title, but computer arrival is asynchronous and exceeded 25 seconds
in one probe. A persistent bounded runner should therefore wait in one room
instead of repeatedly reconnecting.

A 10-second persistent observation-only run subsequently reached `G.F.1` and
recorded a complete two-player, nine-card state. The opponent's most recent
artifact appeared in a left-side action-detail panel while both resource rows
and every local hand hit target remained observable. The unchanged state was
deduplicated across one-second polls, producing one four-event policy snapshot
and a clean wall-clock-limit outcome with zero in-match actions.

The large `Pray` rectangle in the left panel is an action-history display, not
a clickable control; attempts to treat it as a control left the screen
unchanged. At `G.F.1`, an observed `ロキ-67 / Pray` display was followed by the
local artifact-selection phase. Selecting the first weapon through its
revalidated sibling overlay changed the UI to show the selected artifact at
left, a large attack-value display (`ATK12` for Spiritual Staff), an arrow to
the opponent, clickable player rows, and a separate discard artifact. The
runner stopped at its one-click limit before targeting or confirmation.

A later one-click probe selected the plain weapon Punch. Eight seconds after
selection, the action panel still showed `ロキ-67` attacking the sole opponent,
the Punch detail, and `ATK3`, while both player rows remained clickable. This
showed that card selection and action confirmation are separate browser
interactions. Clicking the already selected player row left that frame
unchanged. A separate transparent 310×300 hit target over the selected artifact
panel is the confirmation control: revalidating and clicking it for a plain
Hatchet attack advanced the live Training game from `G.F.1` to `G.F.2`.

An observed defense state separated the same action panel into four spatial
roles: opponent actor, `ロキ-67` target, attack value, and `Forgive` control.
The attack value was neutral-colored. The legal-action layer now permits only
plain armor selection in that narrowly verified neutral-defense shape. It does
not yet attempt elemental compatibility. The red `Forgive` label itself has
`pointer-events: none`; its real hit target is a separate transparent panel.
Clicking that revalidated panel for an identified incoming effect was accepted
and advanced the game. Selecting plain armor during a neutral attack exposes a
parallel right-side artifact panel and `DEF` display; its confirmation contract
was live-verified with both Core Protector and Iron Shield. Each right-panel
confirmation changed normalized state; Core Protector absorbed its attack,
while Iron Shield left a recorded `-1 HP` delta before the game advanced.

A bounded continuous run on the accepted client hash subsequently executed
four recorded inputs without leaving Training: select Hatchet, confirm the
attack on the named sole opponent, Forgive the next incoming effect, and select
Icicle on the following turn. The game advanced from `G.F.1` through `G.F.3`.
This is the first verified multi-turn browser loop; it is still an action-budget
probe, not a completed match or a training dataset.

A later eight-click run recorded accepted state transitions for two weapon
selections, two attack confirmations, two armor selections, and two armor
confirmations while advancing from `G.F.1` through `G.F.4`. Transition events
now preserve before/after state digests, field deltas, and per-player HP deltas.
A briefly observed mirrored post-attack frame remains deliberately blocked
until arrow direction can be represented; its left-side control is not exposed
as a bot action.

A longer bounded run stopped at its 45-second no-progress limit on `G.F.6`.
The local hand had no four-line plain weapon, but it did contain Bouncing Sword,
Reflection Sword, Moonlight Axe, and Angel Sword. Their live Bible entries all
have a fixed `ATK` plus only passive Bounce, Reflect, or Block behavior. The
audited attack allowlist now includes those passive-only shapes (45 weapons in
total, up from 39 plain weapons); random targeting, multi-hit attacks, resource
consumption, status effects, and other active weapon behavior remain excluded.

The next bounded game stopped at `G.F.4` with Legendary Scabbard (`ATK13`,
`DEF1`) and Wind Talons (`ATK1`, Cold on damage) as its remaining weapons. The
captured hand rendered Legendary Scabbard's `ATK13` in the neutral color, so it
was initially added as one explicit dual-use exception. Wind Talons remains
excluded because its elemental status effect has not been modeled.

On 2026-09-07, a targeted non-combat response exposed two left-panel artifacts:
the opponent's Sell action and the selected Heaven Herb. The single-artifact
parser correctly left `action_artifact_asset_path` unset. A stricter generalized
Forgive contract instead preserved and revalidated both context paths, the
named `ロキ-67` target, the unique Forgive label, and its right-side transparent
panel. That click changed normalized state and advanced the Training game. This
supports safely declining targeted multi-artifact interactions without
modeling or accepting the requested trade.

A subsequent 300-second Training run advanced through `G.F.24` and recorded 35
accepted browser transitions before its no-progress cutoff. It stopped on a
hand where Hexagon Doom was the only fixed neutral attack; its official entry
is `ATK11` plus automatic `Dark Cloud on damage`. The same already-verified
fixed-attack selection and confirmation contract applies without another
operator choice. The audited allowlist now includes seven such non-elemental
automatic on-damage weapons (Absorb HP, Hell, Cold, Dream, or Dark Cloud), for
32 fixed neutral weapons total. Random, multi-hit, elemental,
resource-consuming, and self-damaging effects remain excluded.

An official-CPU campaign on 2026-09-08 reached `G.F.12` with 14 accepted
browser interactions before run `0fdbe11b-18d4-44e1-a999-654186046ec8`
stopped at its no-progress limit. The bot was awaiting its own Pray selection
and the hand contained Saver Rod (`ATK2`, `DEF6`) plus attack boosters, but the
browser policy exposed only the boosters because Saver Rod was not the earlier
Legendary Scabbard exception. The exact Bible shape is deterministic and needs
no follow-up choice. All six neutral fixed `ATK`/`DEF` weapons are now admitted
as attack bases; elemental Flaming Roll remains excluded. This brings the
audited fixed neutral attack allowlist to 37 weapons.

Follow-up official-CPU campaigns exposed three additional browser rules. Run
`4e91c520-4ee0-462d-997d-159f429588fd` stopped with an affordable fixed Flame
miracle but no verified browser miracle action. Run
`570d65e2-2175-464b-97e5-caf30350c166` stopped with Spark Bag as its only
attack. An attempted empty Prayer in run
`d06d0f39-b41d-421c-a001-e2a828066378` produced the client's explicit message
`Can not pray because having weapons`; clicking the empty panel is therefore
legal only with a weapon-free hand.

The revised browser catalog admits 80 one-click weapons: 63 exact fixed attacks
and 17 exact probabilistic attacks, across all elements, plus six affordable
fixed-damage miracles. Additive and state-dependent attacks remain excluded.
Run `10722ab9-927a-4a80-9283-cfd9121f53bb` verified that a probabilistic weapon
selection renders its chance expression without a target and requires a
separate panel resolution. Finally, run
`16aff090-919e-46dc-a475-b2074921ebe6` completed an official CPU game through a
classified terminal loss, with accepted elemental attacks, miracle use,
defense, and sparse terminal reward evidence.

### Private API liveness incident — 2026-09-08

Run `bf5825bc-bab5-4d41-b9c5-b401a224f4fb` appeared to hang at `G.F.11`.
The recorded state showed the server awaiting ロキ-67 on an attack turn with
18 HP, 15 MP, an active Dream curse, and no modeled weapon. The legal-action
set was empty even though the reliable hand contained the affordable targeted
`<Dream>` miracle (`addCurse`, model 222). The policy therefore abstained and
waited for the 180-second generic no-progress limit; the service later marked
the player as bot-controlled before the operator interrupted the run.

The reviewed tactical bridge now admits affordable targeted `addCurse`
miracles. It also gives API-validated block, bounce, and reflect defenses their
full prevention value when ranking legal defenses. Any remaining unsupported
self-turn ends an executable run immediately as `unsupported_self_turn`,
preserving the state and decision for the next ruleset extension without
appearing hung.

## Current rules useful to perception and action validation

The live Bible states:

- Non-element attacks can be defended by any element.
- Fire is defended by Water; Water by Fire; Wood by Stone; Stone by Wood.
- Light cannot be defended by any element.
- Darkness can be defended by any element, but damage reduces HP to zero.
- Combining different elements yields Non-element.
- Light can substitute for Fire, Water, Wood, or Stone.
- Diseases naturally worsen each turn with a 5% chance and worsen when
  another disease is caught.
- Under Fog, using an artifact on an enemy chooses the target at random.
- Weapons, Sun Amulet, and Dangerous Mortar cannot be discarded.
- After the Apocalypse, Discard becomes Sacrifice.
- Miracles consume MP, are reusable, and are not subject to Sell, Buy, or
  Nocturnal Broom.
- A bounced attack goes to somebody.
- Earth God uses trade, weapons, armor, and sundries; Moon God uses miracles.
- Guardians act after an enemy turn with a 25% chance and leave with a 10%
  chance when damaged.
- After the Apocalypse, a devil has a 25% chance to appear whenever an
  artifact is given.

The seven artifact catalogs currently total 291 entries:

| Category | Count |
| --- | ---: |
| Weapons | 107 |
| Armor | 78 |
| Sundries | 19 |
| Miracles | 30 |
| Devils | 5 |
| Guardians | 42 |
| Phenomena | 10 |

Representative detail records verified in the live client include Bronze
Club (`ATK1`, `$1`, gift rate `3/500`), God Shield (`DEF30`, `$30`, gift rate
`1/500`), Sun Amulet (restore `HP+10` when HP becomes zero), Fireball
(`+ATK2`, cost `2MP`), Full Moon Blade (`ATK10`), and Solar Eclipse (summon
guardians to everybody).

## Automation implications

1. Use semantic text/input/image observations first and screenshots only as a
   diagnostic or visual fallback. Coordinate-only automation would be brittle
   because the logical viewport is scaled to the available browser area.
2. Treat asset paths (for example, `/images/items/weapons/bronze-club.webp`)
   as useful observation features, but version them against the bundle hash.
3. Maintain a selector contract test that opens the public Bible and verifies
   category counts before allowing a game run.
4. Fail closed on an unknown screen, count drift, duplicate/ambiguous target,
   timeout, CAPTCHA, interstitial, or loss of account state.
5. Do not inspect, call, or emulate undocumented Firebase/cloud-function
   endpoints. The production adapter should exercise only the visible web UI.

### Architecture update — 2026-09-07

The fifth implication above records the original research conclusion. The
operator subsequently selected the unofficial
[`xu-shawn/pygodfield`](https://github.com/xu-shawn/pygodfield) client for
private live-game control. [ADR 0003](../architecture/0003-pygodfield-live-api.md)
documents the amended boundary, exact revision pin, identity migration,
hidden-information filtering, and continued browser role. Public Duel remains
disabled.

At `2026-09-07T06:02:52Z`, the pinned client fetched the live English catalog.
It contained 296 models with content SHA-256
`df182c8a230876886f50ac83a79cf6aa7b737eec7b76279737e4cf6a1dcb6249`:
the 291 visible records counted above plus five records in the `trade`
category. The checksummed capture is in
[`data/snapshots/2026-09-07/api-catalog-en.json`](../../data/snapshots/2026-09-07/api-catalog-en.json).

## Implemented data refresh

`godfield-bot data refresh` now replays the browser extraction into normalized,
versioned JSON, validates every detail panel, separates common category notes,
and fingerprints the shipped client bundle. `godfield-bot data diff` compares
client identity, official rule text, category notes, and every artifact record
while ignoring observation timestamps. Gameplay state/action
schemas require one operator-approved account creation and a read-only
observation session in Training mode.

## Public sources

- Official web client: <https://godfield.net/>
- Official privacy policy: <https://godfield.net/privacy>
- Apple App Store: <https://apps.apple.com/us/app/god-field/id1536427424>
- Google Play: <https://play.google.com/store/apps/details?id=net.godfield&hl=en>
